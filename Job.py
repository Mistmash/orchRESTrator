class Job:
    def __init__(self, tag, increment, time=None):
        self.tag = tag
        self.increment = increment
        self.time = time

    def scheduleJob:
        # creates and executes the command to add this job to the schedule

    def unscheduleJob:
        # creates and executes the command to clear this job from the schedule
